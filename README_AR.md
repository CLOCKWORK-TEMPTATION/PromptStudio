# 🚀 PromptStudio - منصة هندسة الأوامر الذكية

منصة شاملة لهندسة الأوامر الذكية مع توليد SDK تلقائي، نشر سحابي، تعاون فوري، وتخزين مؤقت دلالي.

## ✨ المبدأ المؤسس

**Prompt-as-Code** و **Agentic Systems** مع:
- 🔒 **أمان نوعي كامل** (Type-safety) مع TypeScript + Zod
- 📊 **مخرجات مهيكلة** في جميع الخدمات
- 🛡️ **حوكمة صارمة** مع فحص السلامة والأمان
- 💰 **مراقبة التكلفة والجودة** في الوقت الفعلي

## 🎯 الميزات الرئيسية

### 🎨 محرر الأوامر المتقدم
- محرر مرئي مع دعم المتغيرات
- اختبار فوري ومعاينة النتائج
- تكوين النماذج (temperature, max tokens, top_p)
- دعم متعدد المزودين (OpenAI, Anthropic)
- تحليل ذكي للأوامر مع اقتراحات التحسين

### 🔧 توليد SDK تلقائي
توليد كود عميل جاهز للإنتاج:

#### Python SDK
- وضع async/sync قابل للاختيار
- منطق إعادة المحاولة مع تأخير تصاعدي
- فئات استثناءات مخصصة
- دعم TypedDict للتلميحات النوعية
- توثيق كامل وأمثلة الاستخدام

#### TypeScript SDK
- تعريفات TypeScript كاملة
- async/await مع fetch API
- فئات أخطاء مخصصة
- منطق إعادة المحاولة قابل للتكوين
- دعم الاستجابات المتدفقة

### ☁️ النشر السحابي
نشر بنقرة واحدة على المنصات السحابية الرئيسية:
- **Vercel Edge Functions** - زمن استجابة فائق
- **Cloudflare Workers** - بدء فوري بدون تأخير
- **AWS Lambda** - مع API Gateway
- **Google Cloud Functions** - الجيل الثاني

### 🤝 التعاون الفوري
- **تعاون فوري** عبر WebSocket مع CRDT
- **مؤشرات المستخدمين** - رؤية مكان الكتابة
- **تعديل متزامن** بدون تعارض باستخدام Yjs
- **إدراك الحضور** - معرفة المتصلين
- **تعليقات وملاحظات** على أجزاء محددة
- **مشاركة الجلسات** مع صلاحيات قابلة للتخصيص
- **تحكم في الأدوار** - مالك، محرر، مشاهد
- **سجل التعديلات** مع إدارة الإصدارات

### 💾 التخزين المؤقت الدلالي
- **تفعيل/إلغاء التخزين المؤقت** على مستوى النظام
- **مطابقة بالتشابه** للعثور على إجابات مشابهة
- **عتبة تشابه قابلة للتكوين** (0.5-1.0)
- **تكوين TTL** لانتهاء صلاحية الإدخالات
- **تنظيم بالوسوم** لتصنيف الإدخالات
- **قواعد إبطال التخزين المؤقت**
- **لوحة تحليلات** لمعدلات النجاح والتوفير
- **واجهة إدارة التخزين المؤقت**

### 🔒 ميزات الأمان
- مصادقة مفاتيح API
- تكوين حدود المعدل
- إشعارات webhook للأحداث
- توقيع الطلبات للـ webhooks
- كشف المعلومات الشخصية (PII)
- كشف المحتوى السام
- منع حقن الأوامر
- كشف التحيز
- تحليل الانحراف

## 🛠️ المكدس التقني

### الواجهة الأمامية
- **Next.js 14+** - إطار React مع App Router
- **React 19** - مكتبة واجهة المستخدم
- **TypeScript** - أمان الأنواع
- **Tailwind CSS** - التصميم
- **Zustand** - إدارة الحالة
- **Lucide React** - الأيقونات
- **Socket.IO Client** - عميل WebSocket
- **Yjs** - عميل CRDT

### الخادم الخلفي
- **Node.js** + **Express** - خادم API
- **Socket.IO** - خادم WebSocket للميزات الفورية
- **PostgreSQL** + **Prisma** - قاعدة البيانات و ORM
- **Redis** - التخزين المؤقت و pub/sub
- **Yjs** - تنفيذ CRDT للتحرير التعاوني
- **OpenAI API** - توليد Embeddings للبحث الدلالي

## 🚀 البدء السريع

### المتطلبات الأساسية
- Node.js 18+
- PostgreSQL 15+ (للتعاون)
- Redis 7+ (للتخزين المؤقت)
- Docker & Docker Compose
- مفتاح OpenAI API

### التشغيل السريع

#### Windows
```cmd
# تشغيل سريع
start.bat
```

#### Linux/macOS
```bash
# تشغيل سريع
chmod +x start.sh
./start.sh
```

#### تشغيل يدوي
```bash
# 1. استنساخ المستودع
git clone https://github.com/your-org/prompt-studio.git
cd prompt-studio

# 2. إنشاء ملف البيئة
cp .env.example .env
# عدّل .env وأضف مفتاح OpenAI API

# 3. تثبيت التبعيات
npm install

# 4. تشغيل قواعد البيانات
docker-compose -f docker-compose.dev.yml up -d postgres redis

# 5. تشغيل الترحيلات
npx prisma migrate dev --name init
npx prisma generate

# 6. تشغيل التطبيق
npm run backend:dev  # في terminal منفصل
npm run dev          # في terminal آخر
```

### الوصول للتطبيق
- **الواجهة الأمامية**: http://localhost:3000
- **API الخلفي**: http://localhost:3001
- **فحص الصحة**: http://localhost:3001/health

## 📁 هيكل المشروع

```
PromptStudio/
├── src/
│   ├── app/                    # Next.js App Router
│   ├── components/             # مكونات React
│   ├── backend/               # خادم Express
│   │   ├── api/               # مسارات API
│   │   ├── services/          # منطق العمل
│   │   └── websocket/         # معالجات WebSocket
│   ├── lib/                   # مكتبات أساسية
│   ├── stores/                # إدارة الحالة (Zustand)
│   └── types/                 # تعريفات TypeScript
├── prisma/                    # مخطط قاعدة البيانات
├── docker-compose.dev.yml     # Docker للتطوير
├── docker-compose.prod.yml    # Docker للإنتاج
└── README.md
```

## 🔧 أوامر التطوير

```bash
# تشغيل خادم التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل خادم الإنتاج
npm start

# فحص الأنواع
npm run type-check

# تشغيل الخادم الخلفي فقط
npm run backend:dev

# تشغيل قاعدة البيانات
docker-compose -f docker-compose.dev.yml up -d postgres redis
```

## 🐳 التشغيل بـ Docker

### التطوير
```bash
docker-compose -f docker-compose.dev.yml up
```

### الإنتاج
```bash
docker-compose -f docker-compose.prod.yml up -d
```

## 📊 مسارات API

### الجلسات
- `GET /api/sessions` - قائمة جلسات المستخدم
- `POST /api/sessions` - إنشاء جلسة
- `GET /api/sessions/:id` - تفاصيل الجلسة
- `PATCH /api/sessions/:id` - تحديث الجلسة
- `DELETE /api/sessions/:id` - حذف الجلسة

### التخزين المؤقت
- `GET /api/cache/config` - تكوين التخزين المؤقت
- `PATCH /api/cache/config` - تحديث التكوين
- `POST /api/cache/lookup` - البحث في التخزين المؤقت
- `GET /api/cache/analytics` - تحليلات التخزين المؤقت

### الأوامر
- `GET /api/prompts` - قائمة الأوامر
- `POST /api/prompts` - إنشاء أمر جديد
- `GET /api/prompts/:id` - تفاصيل الأمر
- `PUT /api/prompts/:id` - تحديث الأمر
- `DELETE /api/prompts/:id` - حذف الأمر

### RAG والبحث
- `POST /api/rag/query` - استعلام RAG
- `POST /api/rag/index` - فهرسة المحتوى
- `GET /api/rag/sources` - مصادر البيانات

### السلاسل والتفكير
- `POST /api/chains/execute` - تنفيذ سلسلة أوامر
- `GET /api/reasoning/history` - سجل التفكير
- `POST /api/refinement/improve` - تحسين الأوامر

## 🔌 أحداث WebSocket

### التعاون
- `join_session` - الانضمام لجلسة تعاون
- `leave_session` - مغادرة الجلسة
- `edit_operation` - إرسال تحديث CRDT
- `cursor_move` - تحديث موضع المؤشر
- `sync_state` - مزامنة الحالة الكاملة

### الحضور
- `user_joined` - مستخدم انضم للجلسة
- `user_left` - مستخدم غادر الجلسة
- `presence_update` - تحديث قائمة الحضور
- `cursor_update` - تحديث المؤشر البعيد

## 🧪 الاختبار

```bash
# تشغيل الاختبارات
npm test

# اختبارات مع التغطية
npm run test:coverage

# اختبارات E2E
npm run test:e2e
```

## 📈 المراقبة والتحليلات

- **فحص الصحة**: `/health` - حالة الخدمات
- **المقاييس**: معدلات النجاح، زمن الاستجابة، استخدام التوكنات
- **التحليلات**: إحصائيات الاستخدام، توفير التكلفة
- **السجلات**: سجلات مفصلة لجميع العمليات

## 🔐 الأمان

- **مصادقة JWT** لجميع المسارات المحمية
- **تشفير البيانات الحساسة** في قاعدة البيانات
- **حدود المعدل** لمنع إساءة الاستخدام
- **فحص السلامة** للمحتوى
- **كشف المعلومات الشخصية** وإخفاؤها
- **منع حقن الأوامر** والهجمات الأمنية

## 🤝 المساهمة

نرحب بالمساهمات! يرجى قراءة [دليل المساهمة](CONTRIBUTING.md) للتفاصيل.

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

## 🆘 الدعم

- **التوثيق**: [docs.promptstudio.dev](https://docs.promptstudio.dev)
- **المجتمع**: [Discord](https://discord.gg/promptstudio)
- **المشاكل**: [GitHub Issues](https://github.com/your-org/prompt-studio/issues)
- **البريد الإلكتروني**: support@promptstudio.dev

---

<div align="center">
  <strong>🚀 PromptStudio - مستقبل هندسة الأوامر الذكية</strong>
</div>